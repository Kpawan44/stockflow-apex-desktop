// Intentionally minimal. The app talks directly to Firebase over HTTPS,
// so no privileged Node APIs need to be exposed to the renderer.
// Add contextBridge.exposeInMainWorld(...) here later if you need
// native desktop features (file system, notifications, etc).
